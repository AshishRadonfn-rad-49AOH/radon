let name ='functionup'
let trimName=name.trim()// calls the trim function on a hardcoded string for example ‘ functionUp  ’
let toLower=name.toLowerCase() // changes the case of the string to lower. [Call toLowerCase() on a hardcoded string]
let toUpper=name.toUpperCase() //changes the case of the string to upper case [Call toUpperCase()

module.exports.trimName=trimName
module.exports.toLower=toLower
module.exports.toUpper=toUpper